eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1340/eureka/  #注册中心地址
spring:
  application:
    name: gateway
server:
  port: 1500
#  context-path: /gateway
zuul:
  routes:
    test-server: /test/**

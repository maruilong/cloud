spring:
  application:
    name: relation-service                     #指定了配置文件的应用名
  cloud:
    config:
      uri: http://192.168.1.10:1350/        #Config server的uri
      profile: dev                       #指定的环境
      label: master                   #指定分支
  main:
    allow-bean-definition-overriding: true
server:
  port: 1602
#management:
#  security:
#    enabled: false     #SpringBoot 1.5.X 以上默认开通了安全认证，如果不关闭会要求权限
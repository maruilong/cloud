spring:
  application:
    name: search-service
  cloud:
    config:
      profile: default
      label: master
      uri: http://localhost:1350/
  rabbitmq:
      host: localhost
      port: 5672
      username: mrl
      password: shinian
server:
  port: 1901
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh